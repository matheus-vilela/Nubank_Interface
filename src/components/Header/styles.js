import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerheader: {
    marginHorizontal: 30,
    alignItems: 'center',
    marginBottom: 5,
  },
  header: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  textheader: {
    color: '#fff',
    fontSize: 18,
    marginLeft: 10,
    fontWeight: 'bold',
  },
});

export default styles;
